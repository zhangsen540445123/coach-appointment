<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.umxinli.mapper.TopicDirectionMapper">
    
    <resultMap id="BaseResultMap" type="com.umxinli.entity.TopicDirection">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="value" property="value"/>
        <result column="icon_url" property="iconUrl"/>
        <result column="sort_order" property="sortOrder"/>
        <result column="enabled" property="enabled"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>
    
    <select id="selectAll" resultMap="BaseResultMap">
        SELECT * FROM topic_direction ORDER BY sort_order ASC, id ASC
    </select>
    
    <select id="selectEnabled" resultMap="BaseResultMap">
        SELECT * FROM topic_direction WHERE enabled = 1 ORDER BY sort_order ASC, id ASC
    </select>
    
    <select id="selectById" resultMap="BaseResultMap">
        SELECT * FROM topic_direction WHERE id = #{id}
    </select>
    
    <select id="selectList" resultMap="BaseResultMap">
        SELECT * FROM topic_direction
        <where>
            <if test="keyword != null and keyword != ''">
                AND name LIKE CONCAT('%', #{keyword}, '%')
            </if>
        </where>
        ORDER BY sort_order ASC, id ASC
        LIMIT #{offset}, #{limit}
    </select>
    
    <select id="count" resultType="int">
        SELECT COUNT(*) FROM topic_direction
        <where>
            <if test="keyword != null and keyword != ''">
                AND name LIKE CONCAT('%', #{keyword}, '%')
            </if>
        </where>
    </select>
    
    <insert id="insert" parameterType="com.umxinli.entity.TopicDirection" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO topic_direction (name, value, icon_url, sort_order, enabled, created_at, updated_at)
        VALUES (#{name}, #{value}, #{iconUrl}, #{sortOrder}, #{enabled}, NOW(), NOW())
    </insert>
    
    <update id="update" parameterType="com.umxinli.entity.TopicDirection">
        UPDATE topic_direction SET
            name = #{name},
            value = #{value},
            icon_url = #{iconUrl},
            sort_order = #{sortOrder},
            enabled = #{enabled},
            updated_at = NOW()
        WHERE id = #{id}
    </update>
    
    <delete id="deleteById">
        DELETE FROM topic_direction WHERE id = #{id}
    </delete>
    
    <update id="updateEnabled">
        UPDATE topic_direction SET enabled = #{enabled}, updated_at = NOW() WHERE id = #{id}
    </update>
</mapper>

