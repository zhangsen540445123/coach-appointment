<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.umxinli.mapper.UserFeedbackMapper">
    
    <resultMap id="BaseResultMap" type="com.umxinli.entity.UserFeedback">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="user_name" property="userName"/>
        <result column="content" property="content"/>
        <result column="status" property="status"/>
        <result column="reply" property="reply"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>
    
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user_feedback (user_id, user_name, content, status)
        VALUES (#{userId}, #{userName}, #{content}, 0)
    </insert>
    
    <update id="updateStatus">
        UPDATE user_feedback SET status = #{status}, reply = #{reply}, updated_at = NOW() WHERE id = #{id}
    </update>
    
    <select id="selectById" resultMap="BaseResultMap">
        SELECT * FROM user_feedback WHERE id = #{id}
    </select>
    
    <select id="selectAll" resultMap="BaseResultMap">
        SELECT * FROM user_feedback
        <where>
            <if test="status != null">
                status = #{status}
            </if>
        </where>
        ORDER BY created_at DESC
    </select>
    
    <delete id="deleteById">
        DELETE FROM user_feedback WHERE id = #{id}
    </delete>
</mapper>

