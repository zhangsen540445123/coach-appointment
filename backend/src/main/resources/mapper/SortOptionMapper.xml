<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.umxinli.mapper.SortOptionMapper">
    
    <resultMap id="BaseResultMap" type="com.umxinli.entity.SortOption">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="value" property="value"/>
        <result column="sort_order" property="sortOrder"/>
        <result column="enabled" property="enabled"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>
    
    <select id="selectAll" resultMap="BaseResultMap">
        SELECT * FROM sort_option ORDER BY sort_order ASC, id ASC
    </select>
    
    <select id="selectEnabled" resultMap="BaseResultMap">
        SELECT * FROM sort_option WHERE enabled = 1 ORDER BY sort_order ASC, id ASC
    </select>
    
    <select id="selectById" resultMap="BaseResultMap">
        SELECT * FROM sort_option WHERE id = #{id}
    </select>
    
    <select id="selectList" resultMap="BaseResultMap">
        SELECT * FROM sort_option
        <where>
            <if test="keyword != null and keyword != ''">
                AND name LIKE CONCAT('%', #{keyword}, '%')
            </if>
        </where>
        ORDER BY sort_order ASC, id ASC
        LIMIT #{offset}, #{limit}
    </select>
    
    <select id="count" resultType="int">
        SELECT COUNT(*) FROM sort_option
        <where>
            <if test="keyword != null and keyword != ''">
                AND name LIKE CONCAT('%', #{keyword}, '%')
            </if>
        </where>
    </select>
    
    <insert id="insert" parameterType="com.umxinli.entity.SortOption" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sort_option (name, value, sort_order, enabled, created_at, updated_at)
        VALUES (#{name}, #{value}, #{sortOrder}, #{enabled}, NOW(), NOW())
    </insert>
    
    <update id="update" parameterType="com.umxinli.entity.SortOption">
        UPDATE sort_option SET
            name = #{name},
            value = #{value},
            sort_order = #{sortOrder},
            enabled = #{enabled},
            updated_at = NOW()
        WHERE id = #{id}
    </update>
    
    <delete id="deleteById">
        DELETE FROM sort_option WHERE id = #{id}
    </delete>
    
    <update id="updateEnabled">
        UPDATE sort_option SET enabled = #{enabled}, updated_at = NOW() WHERE id = #{id}
    </update>
</mapper>

